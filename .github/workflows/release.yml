name: Release

on:
  push:
    tags: ['v*']

permissions: {}

jobs:
  publish:
    name: Publish
    uses: praxi-labs/ci/.github/workflows/npm-publish.yml@v1
    with:
      sibling-package: phylax-sdk-js
    permissions:
      contents: read
      id-token: write
